#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a;
    cin >> a;
    // ternary
    (a % 2 == 0) ? cout << "EVEN" : cout << "ODD";
    // if er condition ? if er print : else er print
    return 0;
}